# Local Steering Notes

Use this directory only for project-specific steering that truly must override or extend the global baseline.

Default recommendation:
- keep the corporate rules in the global baseline
- add only project-specific constraints here
- document why the local override exists
